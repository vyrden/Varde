{
  "name": "@varde/module-hello-world",
  "private": true,
  "version": "1.0.0",
  "description": "Module témoin : exerce l'API publique du core (audit, scheduler, config, i18n, ui, events).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit"
  },
  "dependencies": {
    "@varde/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "@varde/config": "workspace:*",
    "typescript": "6.0.3",
    "vitest": "4.1.4"
  }
}
